spring:
  application:
    name: jpa-in-job-app

  datasource:
    url: jdbc:postgresql://localhost:5432/jobapp
    username: postgres
    password: bhavesh18
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true